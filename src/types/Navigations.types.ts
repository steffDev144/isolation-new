export interface NavigationItem {
    text: string;
    id: string;
    href: string;
}

export interface Navigations {
    quests: NavigationItem;
    contacts: NavigationItem;
    certificate: NavigationItem;
    franchising: NavigationItem;
    dubna: NavigationItem;
}